---
- name: Bootstrap cloned image
  tags: bootstap
  hosts: target
  gather_facts: yes
  vars:
    apt_updated: "0"
  roles:
    - bootstrap
    - role: multinic
      when: want_multinic is defined and want_multinic
    - software
    - cleanup
